import * as React from "react"
import { avatar } from "../styles/base.css"

const Avatar: React.FC = () => (
	<img className={ avatar } src="/images/AvatarSquare_Small.jpg" />
)

export default Avatar
